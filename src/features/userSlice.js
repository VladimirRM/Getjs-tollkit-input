import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    firstName:'',
    lastName:'',
}
export const userSlice = ({
    name: 'user',
    initialState,
    reducers:{
        
    }
})